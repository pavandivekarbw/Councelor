// Make sure the path is correct; for example, if the Loader component is in 'src/components/loader/Loader.tsx', use:
import Loader from "../../components/loader/Loader";

export default function Loading() {
    return <Loader />;
}
